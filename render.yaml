services:
  - type: web
    name: fraud-detection
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run fraud_detection.py --server.port=$PORT --server.address=0.0.0.0
